import React from 'react'

import { GameBarContainer } from './game-bar.styles'

const GameBar = ({ children }) => {
  return <GameBarContainer>{children}</GameBarContainer>
}

export default GameBar
